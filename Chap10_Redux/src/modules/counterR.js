
const init = {
    title: 'CounterR',
    num: 0,
}
const counterR = (state = init, action) => {
    switch(action.type) {
        
        default: 
            return state;
    }
}
export default counterR;